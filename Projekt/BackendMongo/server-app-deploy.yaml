apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-mongo-server-app-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: project-mongo-server-app
  template:
    metadata:
      labels:
        app: project-mongo-server-app
    spec:
      containers:
      - image: project-mongo-server-kubernetes-app:latest
        name: container1
        ports:
        - containerPort: 5000
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: server-side-configs
        - secretRef:
            name: server-side-secrets