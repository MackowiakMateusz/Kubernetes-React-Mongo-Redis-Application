apiVersion: v1
kind: Pod
metadata:
  name: mongodb
  labels:
    app: project-mongodb
spec:
  volumes:
  - name: project-mongo-vol
    persistentVolumeClaim:
      claimName: project-pvc
  containers:
  - image: mongo
    name: container1
    command:
    - mongod
    - "--bind_ip"
    - "0.0.0.0"
    ports:
    - containerPort: 27017
    volumeMounts:
    - name: project-mongo-vol
      mountPath: /data/db